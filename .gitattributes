# Normalise to LF in the repo. Written on Windows, run on Linux -- a CRLF
# shebang line makes a script fail on the Pi with a confusing "not found".
* text=auto eol=lf
*.png binary
*.jpg binary
*.wav binary

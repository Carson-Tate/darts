# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Pre-rendered voice clips -- regenerate on the Pi with tools/render_audio.py.
# They're large, binary, and machine-specific; no reason to version them.
sounds/*.wav

# Calibration output and board photos
calibration_check.jpg
calibration_mask.jpg
samples/

# Local overrides -- keep config.yaml tracked, but let the Pi have its own
config.local.yaml

# OS noise
.DS_Store
Thumbs.db
desktop.ini
